server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app fallback so shared /notes/<id> links load the frontend.
    location / {
        try_files $uri $uri/ /index.html;
    }
}